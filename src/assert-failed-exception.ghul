namespace Ghul is
    /// Thrown when a failing `assert` has a string message rather than an exception value.
    class AssertFailedException: System.Exception is
        /// Creates the exception with `message`.
        init(message: string) is
            super.init(message)
        si
    si
si
